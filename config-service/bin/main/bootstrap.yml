server:
  port: 8081
  
 spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maocber/springboot-microservices-my-example.git
          searchPaths: config-data
          username: ${GIT_USER}
          passworf: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
            